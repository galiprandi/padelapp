## 📋 BACKLOG

## ✅ DONE
- [x] 2026-09-03 — Join Slot Invitation Helper Extraction, Match Creator Attribution & Accessibility Polish: Extracted helper functions (`formatJoinSlotInvitationMessage`, `formatStatus`, `teamKeyForPosition`, `defaultTeamLabel`, `MATCH_STATUS`) into `src/app/j/[playerId]/join-slot-utils.ts`. Updated `JoinSlotContent` in `src/app/j/[playerId]/page.tsx` to display match creator attribution with `PlayerAvatar`, render slot user avatars, and add ARIA landmark regions (`role="region"`). Backed by Vitest unit tests in `src/app/j/[playerId]/__tests__/join-slot-utils.test.ts`.
- [x] 2026-09-02 — OnboardingChecklist Progress Helper, ARIA Pending States & Solid MDS Container Styling Polish: Extracted `calculateOnboardingProgress` helper function in `src/components/onboarding-checklist.tsx` with unit tests in `src/components/__tests__/onboarding-checklist.test.ts`. Replaced translucent button borders (`border-primary/30`) and container backgrounds (`bg-primary/10`) across `OnboardingChecklist`, `ProfileForm` (`src/app/(app)/me/profile/profile-form.tsx`), and `DashboardContent` (`src/app/(app)/me/dashboard-content.tsx`) with solid MDS theme container styling (`border-border hover:bg-muted`, `bg-muted text-foreground border border-border shadow-xs`). Added `aria-busy` and context-aware ARIA labels to `JoinSlotButton` (`src/app/j/[playerId]/join-slot-button.tsx`), install, and notification permission buttons during pending transitions.
- [x] 2026-09-01 — MDS Solid Icon Badge Container Styling, ARIA Accessibility & Unit Tests for EmptyState: Refactored `EmptyState` (`src/components/empty-state.tsx`) to add a solid MDS icon badge container (`bg-muted text-muted-foreground border border-border shadow-xs`), `role="region"`, and `aria-label={title}` accessibility attributes. Backed by unit tests in `src/components/__tests__/empty-state.test.ts`.
- [x] 2026-08-31 — SignInButton Tactile Press Scaling, Keyboard Focus Rings & Dynamic ARIA Feedback: Refactored `SignInButton` (`src/components/auth/sign-in-button.tsx`) to add tactile active press scaling (`active:scale-[0.98] transition-all`), explicit focus-visible ring offset styling (`focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 ring-offset-background`), class merging via `cn()`, dynamic `aria-busy={pending}`, and localized pending `aria-label` screen reader updates. Backed by unit tests in `src/components/auth/__tests__/sign-in-button.test.ts`.
- [x] 2026-08-30 — PWA Install Banner Styling, Accessibility & Storage Helpers Refactor: Refactored `PwaInstallBanner` (`src/components/pwa-install-banner.tsx`) with solid MDS primary CTA styling (`bg-primary text-primary-foreground hover:bg-primary/90 font-bold`), `aria-busy={isInstalling}`, dynamic ARIA labels during installation, and exported localStorage helper functions (`isPwaBannerDismissed`, `dismissPwaBanner`, `clearPwaBannerDismissal`). Backed by unit tests in `src/components/__tests__/pwa-install-banner.test.ts`.
- [x] 2026-08-29 — Tactile Active Scaling, Keyboard Focus Rings & Unit Tests for PWA Install Components: Enhanced `InstallButton` (`src/components/share/install-button.tsx`) with tactile active press scaling (`active:scale-[0.98] transition-all`), focus-visible ring offsets (`focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 ring-offset-background`), `aria-busy={isInstalling}`, and explicit ARIA labels. Created unit tests in `src/components/share/__tests__/install-content.test.ts` verifying user agent platform detection logic and Argentine Spanish platform step labels.
- [x] 2026-08-28 — MDS Solid Container Styling & Tactile Accessibility Polish across Profile, Dashboard & Onboarding Checklist: Standardized incomplete profile banners, pending attendance badges, substitute cards, preferred side pills, and category badges across `/me` (`src/app/(app)/me/dashboard-content.tsx`), `/me/profile` (`src/app/(app)/me/profile/profile-form.tsx`), and `OnboardingChecklist` (`src/components/onboarding-checklist.tsx`) using solid MDS container styling (`bg-amber-100...`, `bg-emerald-100...`, `bg-muted...`), active tactile press scaling (`active:scale-[0.98]`), and keyboard focus ring offsets (`ring-offset-background`).
- [x] 2026-08-23 — MDS Solid Container Styling & Micro-UX Polish in Join Slot, Public Profile & PWA Install Navigation: Standardized status badges, viewer slot highlights, and error cards in `/j/[playerId]` (`src/app/j/[playerId]/page.tsx`) using solid MDS container styling (`bg-emerald-100...`, `bg-amber-100...`, `bg-card border-primary shadow-xs`). Enhanced `JoinSlotButton` (`src/app/j/[playerId]/join-slot-button.tsx`) with `h-12` height, active tactile press scaling (`active:scale-[0.98] transition-all`), focus-visible ring offsets, and `Loader2` spinner animation during pending state. Refactored streak badge and WR pill in `/p/[userId]` (`src/app/p/[userId]/page.tsx`) with solid MDS styling (`bg-amber-100...`, `bg-muted`), and added tactile scaling and keyboard focus rings to `InstallLinkButton` (`src/components/pwa-install-link.tsx`) and `InstallPage` (`src/app/install/page.tsx`).
- [x] 2026-08-22 — Court Side Preference Selector in Profile & Player Graph Integration: Extended `updateUserProfileAction` (`src/app/(app)/me/actions.ts`) to support `preferredSideInput` ('RIGHT' | 'LEFT' | 'BOTH' | null), persisting 'RIGHT'/'LEFT'/null in `playerGraphStats` and returning 'BOTH' for non-biased/both-side preference. Updated `getEditableProfile` (`src/lib/queries/profile.ts`) to fetch user's preferred side with 'BOTH' fallback. Added interactive court side selection chips, accessible `role="radiogroup"` / `role="radio"` attributes, dynamic `FormMiniCourtIndicator` position preview, and toast notifications in `ProfileForm` (`src/app/(app)/me/profile/profile-form.tsx`). Backed by unit tests in `src/lib/__tests__/profile-side.test.ts`. (PR #roby/profile/court-side-preference-selector)
- [x] 2026-08-21 — Interactive Category Selection Chips & Category Descriptions: Created `CATEGORIES` definitions and `getCategoryDefinition` helper in `src/lib/constants/categories.ts`. Extended `getEditableProfile` (`src/lib/queries/profile.ts`) and `updateUserProfileAction` (`src/app/(app)/me/actions.ts`) with level validation (1–8). Updated `ProfileForm` (`src/app/(app)/me/profile/profile-form.tsx`) with interactive category chips (1ª Cat. to 8ª Cat.), category description card, MDS styling, accessible `role="radiogroup"` / `role="radio"` attributes, and instant toast notifications ("Categoría actualizada a Xª Cat."). Backed by unit tests in `src/lib/__tests__/categories.test.ts`.
- [x] 2026-08-20 — Player Category Level Badges & Dashboard/Profile Integration: Created `getLevelBadgeLabel` helper mapping user levels (1–8) to standard Argentine Padel category labels (`1ª Cat.` to `8ª Cat.`), backed by Vitest unit tests in `src/lib/__tests__/utils.test.ts`. Selected `users.level` in `getDashboardUserStats` query (`src/lib/queries/dashboard.ts`), and updated `Greeting` (`src/components/greeting.tsx`), `DashboardContent` (`src/app/(app)/me/dashboard-content.tsx`), and `PublicProfileContent` (`src/app/p/[userId]/page.tsx`) to display the MDS-compliant category badge.
- [x] 2026-08-19 — Profile Action Buttons Tactile Polish & Player Avatar Unit Tests: Refactored photo action buttons in `ProfileForm` to add active press tactile scaling (`active:scale-[0.98] transition-all`), focus ring offset styling, and explicit screen reader `aria-label` attributes. Created comprehensive unit tests in `src/components/players/__tests__/player-avatar.test.ts` testing `getPlayerInitials` sanitization, Spanish accents/eñes, single/multi-word names, and special character filtering. (PR #roby/profile/avatar-tactile-polish-and-tests)
- [x] 2026-08-17 — PWA & Push Notification Banner Tactile Polish: Standardized `PushPermissionPrompt` and `PwaInstallBanner` with active tactile scaling (`active:scale-[0.98] transition-all`), explicit focus-visible ring styles for keyboard accessibility, and proper `aria-hidden` attributes for decorative icons. (PR #roby/pwa/banner-tactile-polish)
- [x] 2026-08-16 — Instant Profile Auto-Save & Onboarding Navigation Guard: Added `onBlur` event handler to the profile alias input in `ProfileForm` for immediate saving on focus loss, refactored `saveAlias` to return a `Promise<boolean>`, and updated the onboarding CTA button to await pending alias saves before routing to `/me`. Replaced synchronous `setState` in `useEffect` with lazy `useState` initializers, and added `pb-20` padding to `/me/profile` to keep CTAs clear of the fixed bottom navigation bar. (PR #roby/profile/instant-auto-save-onboarding-guard)
- [x] 2026-08-15 — H1 Header MDS Standardization on Login & Install Pages: Removed non-compliant `tracking-tight` class from primary `<h1>` headings in `/login` (`src/app/login/page.tsx`) and `/install` (`src/app/install/page.tsx`) to strictly align with Minimal Design System (MDS) §1.8 specifications. (PR #roby/pwa/h1-header-mds-standardization)
- [x] 2026-08-14 — Mini-Court Court Side Position Indicator on Public Profile: Integrated an interactive, mobile-first SVG/CSS mini-court diagram (`MiniCourtIndicator`) in the public profile screen (`/p/[userId]`) next to "Lado preferido" to visually highlight the player's primary court position (Derecha, Revés, or Alterno) in primary yellow theme while keeping full screen-reader accessibility. (PR #roby/profile/court-side-indicator)
- [x] 2026-08-13 — Public Profile Accessibility & High-Fidelity Mocking under Bypass: Standardized recent form indicators inside the public player profile `/p/[userId]` (`src/app/p/[userId]/page.tsx`) by adding a dynamic, Spanish-localized ARIA label describing the player's recent form (e.g., `'Forma reciente: G, P'`) and hiding the visual circle indicator dots using `aria-hidden="true"`, fully complying with `DESIGN.md` §2.9. Additionally, refactored `getConfirmedMatchesForProfile` inside `src/lib/queries/match.ts` to return structured, high-fidelity mock matches under `AUTH_BYPASS` or `MOCK_AUTH` conditions instead of an empty array, casting the mock array to the exact typed database query structure via a private helper `getConfirmedMatchesFromDb` to ensure type-safe compiling and rich visuals in E2E tests. (PR #roby/profile/accessibility-mocking)
- [x] 2026-08-10 — Public Profile Header & Onboarding Banner MDS Standardization: Standardized the "Red y Posición" card header inside the public player profile (`src/app/p/[userId]/page.tsx`) to comply with §1.8 of `DESIGN.md` by removing uppercase and tracking-wider styling. Additionally, refactored the onboarding welcome banner in `ProfileForm` (`src/app/(app)/me/profile/profile-form.tsx`) from a transparent border/background (`border-primary/30 bg-primary/5`) to a beautiful, solid container (`border-border bg-card shadow-sm`) aligned with §1.3 and §4.3 of MDS. (PR #roby/profile/mds-header-banner-standardization)
- [x] 2026-08-09 — Profile Alias Validation, Spanish Localization & State Synchronization: Standardized and translated user profile update server action errors in `src/app/(app)/me/actions.ts` into natural Argentine Spanish (voseo). Refined alias input validation with a regex allowing only letters, accents, numbers, spaces, and hyphens to prevent invalid submissions. Sourced the server-formatted alias on successful saving to immediately update and synchronize the client form's local state, preventing visual differences between typed input and database-persisted results. (PR #roby/profile/alias-validation-polish)
- [x] 2026-08-03 — Correct Game Level Selector Initialization: Resolved a TypeScript type-checking and production build-breaking error by correctly passing the user's initialLevel prop (`initialLevel={user.level}`) to the `<ProfileForm />` component on the profile edit page (`/me/profile`). (PR #roby/profile/fix-level-initialization)
- [x] 2026-08-02 — Profile Form initialLevel Prop Initialization Fix: Fixed a blocking compilation and build failure by correctly passing the `initialLevel` required property to the `ProfileForm` component inside `src/app/(app)/me/profile/page.tsx`. (PR #roby/profile/fix-profile-initial-level-prop)
- [x] 2026-07-31 — Stable Bypass Auth & Next.js 16 PPR Dynamic Prerendering Restoration: Standardized the session expiration to a static date and integrated asynchronous `cookies()` signaling inside `auth()`'s bypass flow to resolve Next.js 16's strict prerendering bails, enabling a seamless production build pipeline. (PR #roby/profile/stable-bypass-auth-ppr)
- [x] 2026-07-30 — Biometric Login Restoration, Back Navigation Standardization & Profile Centering Layout: Reinstated PasskeyLoginButton on the login screen, standardized magic-link back-navigation with MDS keyboard focus rings, and resolved public profile layout shifting with an absolute-positioned ShareButton. (PR #roby/profile/onboarding-ux-refinements)
- [x] 2026-07-17 — Setup inicial del agente (sistema .ants creado)
- [x] 2026-07-18 — Mejoras en Onboarding y Perfil: Onboarding prompt en /me, dual CTA en agenda vacía, descripciones de niveles de juego y selector de avatar interactivo con presets de Dicebear. (PR #1 — merged)
- [x] 2026-07-19 — Foto original de Google: Ofrecer selector para restablecer a la foto original de Google decodificando el `id_token` de la cuenta vinculada del usuario.
- [x] 2026-07-20 — Adopción completa de Cache Components (PPR) en el módulo de Perfil, Login y Onboarding. (PR #roby/profile/cache-components-onboarding)
- [x] 2026-07-21 — Compartición nativa de perfil y navegación contextualizada del perfil público. (PR #roby/profile/share-and-back-navigation)
- [x] 2026-07-22 — Estandarización de banners de onboarding y guía interactiva de instalación PWA: Rediseño accesible y adaptativo para iOS y Android siguiendo las Semantic Maxims 1.3, 2.1 y 4.3 de MDS. (PR #roby/pwa/install-onboarding-ux)
- [x] 2026-07-23 — Consolidación de onboarding en lista de preparación: Implementación del componente OnboardingChecklist en la página de inicio para nuevos usuarios. (PR #roby/profile/unified-onboarding-checklist)
- [x] 2026-07-24 — Sólidos visuales en Onboarding y Dashboard: Refactorización de fondos y bordes en checklist de onboarding, instrucciones PWA, y tarjetas de acción del Dashboard para eliminar toda semi-transparencia y cumplir plenamente las directivas de MDS. (PR #roby/profile/dashboard-solid-onboarding-polish)
- [x] 2026-07-25 — Limpieza y validación de tokens de sesión FCM en suscripción: Prevención de filtración de datos cruzados entre múltiples usuarios registrando la misma clave de dispositivo FCM en entornos compartidos. (PR #roby/profile/fcm-token-cleanup-and-validation)
- [x] 2026-07-26 — Customización y eliminación de avatar: Implementación de la opción 'Quitar foto' para limpiar la foto de perfil y volver a las iniciales del nombre de usuario, con soporte para deshacer la acción (Undo) mediante toast. (PR #roby/profile/remove-avatar-initials)
- [x] 2026-07-27 — Cumplimiento de imágenes del sistema de diseño (MDS): Reemplazo de etiquetas nativas <img> por componentes <Image> de Next.js en las páginas de landing, login e instalación. (PR #roby/pwa/standardize-image-components)
- [x] 2026-07-28 — Google Logo reinstating & Back Button Standardization: Added standard back-navigation header links to Profile and Security pages, and reinstated the Google brand logo inside OAuth SignInButton for visual micro-UX clarity. (PR #roby/profile/logo-and-back-navigation)
- [x] 2026-07-29 — Google Account Details display & Welcome Guide Restoration Setting: Enhanced the profile settings form with read-only synced account info (Name, Email) and added a dedicated control to restore the dismissed Onboarding Checklist (Welcome Guide) for new users. (PR #roby/profile/onboarding-restoration-settings)
- [x] 2026-07-30 — Biometric Login Access integration on primary Login page: Added PasskeyLoginButton to the main unauthenticated login layout under the Google OAuth connector to allow registered users to enter with Face ID / Touch ID immediately. (PR #roby/pwa/passkey-login-onboarding)

## 🧠 APRENDIZAJES
## 2026-09-03 - Join Slot Invitation Helper Extraction, Match Creator Attribution & Accessibility Polish
**Learning:** Extracting join slot invitation message formatting logic (`formatJoinSlotInvitationMessage`) into pure utility modules enables precise unit testing across cases where the match creator's name is present, whitespace-only, or missing. Rendering the match creator's avatar alongside localized invitation text ("Agustín Tapia te invitó a sumarte como Pareja A") on direct magic link join pages (`/j/[playerId]`) establishes personal context and trust before players confirm their slot.
**Action:** Always provide explicit creator attribution with player avatars on direct match invitation landing pages.

## 2026-09-02 - OnboardingChecklist Progress Helper, ARIA Pending States & Solid MDS Container Styling Polish
**Learning:** Extracting pure onboarding progress calculation logic (`calculateOnboardingProgress`) into testable helpers enables direct Vitest unit testing across step completion boundaries without rendering React DOM trees. Replacing translucent border classes (`border-primary/30`) and background opacity utilities (`bg-primary/10`) with solid MDS theme container styling (`border-border hover:bg-muted`, `bg-muted text-foreground border border-border shadow-xs`) ensures strict compliance with MDS §1.3 and §4.3 across profile, onboarding, and dashboard views.
**Action:** Extract progress calculation helpers into exported functions for unit testing and maintain solid MDS theme container styling across onboarding and profile views.

## 2026-09-01 - MDS Solid Icon Badge Container Styling, ARIA Accessibility & Unit Tests for EmptyState
**Learning:** Enclosing empty state icons in solid MDS badge containers (`mb-3 flex h-10 w-10 items-center justify-center rounded-full bg-muted text-muted-foreground border border-border shadow-xs`) creates visual hierarchy and consistency across empty screens. Adding `role="region"` and `aria-label={title}` accessibility attributes ensures screen readers clearly identify and announce empty state feedback blocks as landmark sections.
**Action:** Use solid MDS badge containers for icons in empty state components and provide `role="region"` landmark attributes.

## 2026-08-31 - SignInButton Tactile Press Scaling, Keyboard Focus Rings & Dynamic ARIA Feedback
**Learning:** Primary entry OAuth buttons (like `SignInButton`) require tactile active press scaling (`active:scale-[0.98] transition-all`) and keyboard focus ring offset utilities (`focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 ring-offset-background`) for physical tactile feedback and WCAG compliance. Exporting class generation and ARIA calculation helpers (`getSignInButtonClasses`, `getSignInButtonAriaProps`) enables strict unit test coverage of dynamic pending states (`aria-busy`, localized `aria-label`) and class merging behavior without needing heavy DOM rendering mocks.
**Action:** Extract class merging and ARIA calculation helpers from OAuth and entry button components for direct Vitest unit testing.

## 2026-08-30 - PWA Install Banner Styling, Accessibility & Storage Helpers Refactor
**Learning:** Primary call-to-action triggers on entry and dashboard banners (like the installation CTA in `PwaInstallBanner`) require solid primary MDS styling (`bg-primary text-primary-foreground font-bold`) to establish visual hierarchy over secondary actions. Exporting pure storage helper functions (`isPwaBannerDismissed`, `dismissPwaBanner`, `clearPwaBannerDismissal`) allows clean unit test coverage with Vitest global window/localStorage mocks while keeping component state logic predictable.
**Action:** Use solid primary button variants for key conversion CTAs on banners and extract storage side effects into unit-testable helper functions.

## 2026-08-29 - Tactile Active Scaling, Keyboard Focus Rings & Unit Tests for PWA Install Components
**Learning:** PWA installation CTAs (like `InstallButton`) on touch devices require tactile active press feedback (`active:scale-[0.98] transition-all`) and keyboard focus-visible ring offsets (`focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 ring-offset-background`) for physical tactile feedback and WCAG keyboard accessibility. Setting `aria-busy={isInstalling}` and explicit localized `aria-label` attributes guarantees clear screen reader announcements during installation states.
**Action:** Always apply tactile press scaling, explicit focus ring offsets, and `aria-busy` indicators to PWA install action triggers.

## 2026-08-23 - MDS Solid Container Styling & Micro-UX Polish in Join Slot, Public Profile & PWA Install Navigation
**Learning:** According to Minimal Design System (MDS) §1.3 and §4.3, UI badges, status indicators, and notification/error banners must use solid container styling (such as `bg-emerald-100...` or `bg-card border-border shadow-xs`) rather than translucent background opacity utilities (`bg-*-500/10` or `bg-primary/5`). Furthermore, updating join action buttons with explicit touch height (`h-12`), tactile active press scaling (`active:scale-[0.98] transition-all`), focus-visible ring offsets, and pending spinner indicators provides instant feedback and accessible interaction on mobile touchscreens.
**Action:** Replace translucent background utilities on badges and cards with solid MDS container styles, and standardize tactile feedback and focus rings across all secondary action CTAs.

## 2026-08-22 - Court Side Preference Selector in Profile & Player Graph Integration
**Learning:** When adding interactive court side preferences (Derecha, Revés, Ambos) to user profiles without schema modifications where the DB column uses a binary side enum (`"RIGHT"` | `"LEFT"` | `null`), mapping `"BOTH"` to `null` in DB writes and mapping `null` back to `"BOTH"` in DB reads guarantees lossless UI state persistence and page reload consistency. Pairing the selector with a dynamic mini-court court side position preview and accessible `role="radiogroup"` / `role="radio"` attributes ensures instant visual feedback and WCAG compliance.
**Action:** Map non-binary preference options like `"BOTH"` to nullable database enum fields and provide fallback mapping on query reads.

## 2026-08-21 - Interactive Category Selection Chips & Category Descriptions
**Learning:** Providing interactive level/category selection chips (from 1ª Cat. to 8ª Cat.) with immediate contextual descriptions and toast notification feedback in `/me/profile` empowers padel players to accurately self-classify their playing level. Wrapping the chips in an accessible `role="radiogroup"` with `role="radio"` and `aria-checked` attributes satisfies WCAG compliance, while maintaining `levelRef.current` ensures async alias auto-saves preserve updated level values without race conditions.
**Action:** Use `role="radiogroup"` / `role="radio"` for chip selectors and maintain a level ref when integrating level choices with background debounced form auto-saves.

## 2026-08-20 - Player Category Level Badges & Dashboard/Profile Integration
**Learning:** Displaying localized category badges (such as `6ª Cat.`) next to user names in dashboard greetings and public profile headers provides immediate competitive identity and context for padel players in Argentina without cluttering the UI shell. When adding database columns to cached query functions (`getDashboardUserStats`), ensure the field is selected in both mock bypass objects and SQL select queries.
**Action:** Always include player category badges (`getLevelBadgeLabel`) alongside greetings and public profile summaries, and keep query selections aligned across cached query wrappers.

## 2026-08-19 - Profile Action Buttons Tactile Polish & Player Avatar Unit Tests
**Learning:** Action text buttons inside forms require tactile active feedback (`active:scale-[0.98] transition-all`) and explicit screen reader labels (`aria-label`) so that both touch feedback on mobile screens and screen reader accessibility are maintained without visual clutter.
**Action:** Include `active:scale-[0.98] transition-all` and descriptive `aria-label`s on action buttons across profile setting forms.

## 2026-08-17 - PWA & Push Notification Banner Tactile Polish
**Learning:** Banner components on dashboard views (like `PushPermissionPrompt` and `PwaInstallBanner`) benefit from uniform tactile feedback (`active:scale-[0.98] transition-all`) and explicit keyboard focus-visible ring offsets. Hiding purely decorative icons from screen readers with `aria-hidden="true"` ensures clean accessibility without speech redundancy.
**Action:** Always include `active:scale-[0.98] transition-all` and explicit focus-visible rings on interactive banner CTAs, and mark decorative icons with `aria-hidden="true"`.

## 2026-08-16 - Instant Profile Auto-Save & Onboarding Navigation Guard
**Learning:** In auto-saving forms where user actions trigger asynchronous server actions during onboarding transitions, executing client router navigation (`router.push('/me')`) synchronously before the async mutation resolves risks dropping pending alias changes or throwing unhandled unmount errors. Returning a `Promise<boolean>` from the save handler and awaiting its completion before routing guarantees data persistence. Additionally, reading client storage (e.g., `localStorage`) directly inside lazy `useState` initializers prevents cascading synchronous `setState` re-renders in `useEffect`.
**Action:** Always return a Promise from auto-save handlers so navigation actions can await completion before transitioning, and use lazy `useState` initializers when initializing state from `localStorage`.

## 2026-08-15 - H1 Header MDS Standardization on Login & Install Pages
**Learning:** According to MDS §1.8, flat headers (`<h1>` and `<p>`) must adhere to clean typography rules without non-standard letter-spacing adjustments like `tracking-tight` or `uppercase` unless explicitly specified. Standardizing page titles across authentication and entry pages like `/login` and `/install` ensures consistent visual hierarchy and typographic harmony across the entire mobile application shell.
**Action:** Always check `<h1>` element classes during UI audits to confirm they avoid unnecessary `tracking-*` or `uppercase` utility classes.

## 2026-08-14 - Mini-Court Court Side Position Indicator
**Learning:** Visual indicators that mirror real-world sports contexts (such as a miniature padel court diagram highlighting left/Revés vs right/Derecha sides) provide instantaneous cognitive recognition for competitive players without cluttering the screen. By marking graphic sub-elements with `aria-hidden="true"` and relying on surrounding descriptive semantic text (`networkStats.preferredSide`), the UI satisfies both high visual fidelity and WCAG/MDS accessibility requirements.
**Action:** Use clean, minimal SVG/CSS sub-diagrams when representing tactical or spatial preferences (like court side), keeping decorative shapes hidden from screen readers.

## 2026-08-13 - Public Profile Accessibility & High-Fidelity Mocking under Bypass
**Learning:** Adding screen-reader accessibility metadata (`aria-label`) that translates visual indicator patterns (such as green and red dots representing wins and losses) into localized text (e.g., `'Forma reciente: G, P'`) is crucial for compliance with modern accessibility standards like WCAG and the Minimal Design System (§2.9). By pairing this with `aria-hidden="true"` on the visual sub-indicators, we avoid screen-reader noise while preserving the full visual context. Furthermore, when implementing mock fallback layers for local/bypass modes, casting mock objects to the exact TypeScript return type using `Awaited<ReturnType<typeof privateDbHelper>>` prevents dynamic parameter loss and eliminates implicit `any` parameter compilation warnings at the page consumption sites.
**Action:** Always provide explicit, translated screen-reader metadata for icon grids or visual status matrices, and always type-cast mock bypass payloads to their exact database-helper return type instead of using `any`.
